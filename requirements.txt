# Read the Docs build dependencies for the adacovex manual.
#
# The docs live under docs/ as Markdown and are built with Sphinx (default
# theme) using the MyST parser so the existing .md pages compile unchanged.
# Read the Docs installs these (see .readthedocs.yaml, `python.install`) to
# run `sphinx-build` over docs/.
sphinx
myst-parser